# -*- coding: utf-8 -*-
"""
Created on Tue Aug 27 17:10:19 2019

@author: rique
"""
print ("Utilizando los mismos comandos del video, el programa arrojaba un error producto del primer comando (from skimage import), por lo cual este desarrollo se trabajo en forma de comentario")

# Para hacer visible una imagen

#from skimage import  

#foto = io.imread('descarga.jpg') # Este comando permite hacer una lectura de la imagen
#print (type(foto))
#print (foto.shape) # Permite ver la matriz de la imagen

#print (foto.shape) # para leer  la matriz
#import matplotlib.pyplot as plt #esta funcion se utiliza para el procesamiento a imagen
#plt.imshow(foto) # mediante este comando la imagen debiese hacerse visible
#plt.imshow(foto[: :-1]) #Con este comando las filas son invertidas por lo cual la foto debiese verse al reves
#plt.imshow(foto[:, : :-1]) #Este comando da vuelta las columnas por llo cual la imagen debiese verse como espejo
#plt.imshow(foto[50:150, 150:280]) #Este comando se utiliza para cortar un fragmento de la imagen
#plt.imshow(foto[: :2, : :2]) #Este comando reduce la imagen a la mitad


#foto_sin =np.sin(foto) , permitiria calcular el sin de cada valor de la matriz, es irrelevante al trabajar con imagenes


# Conjunto de comandos para trabajar con los valores de la matriz foto
#print (np.sum(foto))
#print (np.prod(foto))
#print (np.mean(foto))
#print (np.std(foto))
#print (np.var(foto))
#print (np.min(foto))
#print (np.max(foto))
#print (np.argmin(foto))
#print (np.argmax(foto))
